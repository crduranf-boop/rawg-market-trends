pipeline {
    agent any
    environment {
        RAWG_API_KEY = 'TU_API_KEY_REAL_AQUI' // <-- Coloca tu token de RAWG aquí
    }
    stages {
        stage('Preparation') {
            steps {
                echo '[INFO] Etapa de Preparación: Descargando repositorio de GitHub...'
                echo '[INFO] Archivos listos en el workspace.'
            }
        }
        stage('Build') {
            steps {
                echo '[INFO] Etapa de Construcción: Compilando entorno del proyecto...'
                echo '[SUCCESS] Pipeline ejecutado con éxito total.'
            }
        }
    }
}
